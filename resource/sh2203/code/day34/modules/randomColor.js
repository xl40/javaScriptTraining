// 随机颜色模块需要依赖随机数模块

// define的第一个参数，是数组，数组内放置当前模块要依赖的其他模块
// define的第二个参数，是回调函数，用来包裹当前模块的功能；回调函数的形参依次为参数1数组内依赖模块的返回功能

define(["./random"], (a)=>{ 
    // console.log(a);
    return function(){
        return `rgb(${a(0,255)},${a(0,255)},${a(0,255)})`
    };
})